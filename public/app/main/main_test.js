'use strict';

describe('app.start module', function() {

  beforeEach(module('app.start'));

  describe('start controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var startCtrl = $controller('startCtrl');
      expect(startCtrl).toBeDefined();
    }));

  });
});